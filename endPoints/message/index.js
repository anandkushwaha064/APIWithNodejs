exports.sendMessage = async (MobileNumber, Message) => {
  console.log(`Sending Message : ${Message} to mobile Number ${MobileNumber}`);
};

exports.sendWhatsapp = async (MobileNumber, Message) => {
  console.log(`Sending Message : ${Message} to mobile Number ${MobileNumber}`);
};

exports.sendWhatsapp = async (Emailid, EmailMessage) => {
  console.log(`Sending Email : ${EmailMessage} to Email ID ${Emailid}`);
};
